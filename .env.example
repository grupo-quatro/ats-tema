# Gmail credential seed (scripts/seed-gmail-credential.mjs)
# Obtener tokens conectando Gmail desde la UI → Firestore → users/{uid}.gmailCredential
SEED_GMAIL_UID=
SEED_GMAIL_ACCESS_TOKEN=
SEED_GMAIL_REFRESH_TOKEN=
